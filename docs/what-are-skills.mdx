---
title: "什么是技能？"
description: "Agent Skills 是一种轻量级、开放的格式，用于通过专业知识和工作流扩展 AI 智能体的能力。"
---

# 什么是技能？

> Agent Skills 是一种轻量级、开放的格式，用于通过专业知识和工作流扩展 AI 智能体的能力。

从核心来看，技能是包含 `SKILL.md` 文件的文件夹。该文件包含元数据（至少包括 `name` 和 `description`）和告诉智能体如何执行特定任务的指令。技能还可以捆绑脚本、模板和参考资料。

```
my-skill/
├── SKILL.md          # 必需：指令 + 元数据
├── scripts/          # 可选：可执行代码
├── references/       # 可选：文档
└── assets/           # 可选：模板、资源
```

## 技能如何工作

技能使用**渐进式披露**来高效管理上下文：

1. **发现**：启动时，智能体只加载每个可用技能的名称和描述，刚好足够知道什么时候可能相关。

2. **激活**：当任务与技能描述匹配时，智能体将完整的 `SKILL.md` 指令加载到上下文中。

3. **执行**：智能体遵循指令，根据需要可选地加载引用的文件或执行捆绑的代码。

这种方法让智能体保持快速，同时按需提供更多上下文。

## SKILL.md 文件

每个技能都以包含 YAML 前置数据和 Markdown 指令的 `SKILL.md` 文件开始：

```mdx
---
name: pdf-processing
description: 提取 PDF 文本、填充表单、合并文件。处理 PDF 时使用。
---

# PDF 处理

## 何时使用此技能
当用户需要处理 PDF 文件时使用此技能...

## 如何提取文本
1. 使用 pdfplumber 进行文本提取...

## 如何填充表单
...
```

`SKILL.md` 顶部需要以下前置数据：

- `name`：一个简短的标识符
- `description`：何时使用此技能

Markdown 正文包含实际指令，对结构或内容没有特定限制。

这种简单格式有一些关键优势：

- **自文档化**：技能作者或用户可以阅读 `SKILL.md` 并理解它的作用，使技能易于审计和改进。

- **可扩展**：技能可以从简单的文本指令到可执行代码、资产和模板，复杂度不一。

- **可移植**：技能只是文件，因此易于编辑、版本控制和共享。

## 下一步

- [查看规范](specification) 了解完整格式。
- [为你的智能体添加技能支持](client-implementation/adding-skills-support) 构建兼容的客户端。
- [在 GitHub 上查看示例技能](https://github.com/anthropics/skills)。
- [阅读编写最佳实践](skill-creation/best-practices) 了解如何编写有效的技能。
- [使用参考库](https://github.com/agentskills/agentskills/tree/main/skills-ref) 验证技能并生成提示 XML。
